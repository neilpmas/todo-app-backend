spring:
  flyway:
    enabled: true
    url: ${DATABASE_URL}
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
    schemas: app
    default-schema: app
    create-schemas: true

